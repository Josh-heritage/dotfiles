# dotfiles
Some of my dot files for my main arch device. They should work on any linux distribution as long as you have the relevant packages installed. Dot files are normally stored in .config. For instance if you use hyprland, its stores in $.config/hypr/hyprland.conf
